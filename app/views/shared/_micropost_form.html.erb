<%= form_for(@micropost) do |f| %>
  <%= render "shared/flash" %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new micropost...", required: true, class: "form-control"%>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
